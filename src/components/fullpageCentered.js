import React from "react"
import Logo from "./logo"
import styles from "./fullpageCentered.module.css"

export default () => (
  <div className={styles.fullpage}>
    <Logo headerText="Karate" />
  </div>
)
